import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container_comment: {
    flex: 1,
    flexDirection: "row",
    margin: 5,
    padding: 5,
  },
});

export default styles;
